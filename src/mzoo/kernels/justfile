# invoke from repo root: just src/mzoo/kernels/ <recipe>

# tilelang needs a CUDA 13 nvcc for sm121 (/usr/bin/nvcc is 12.0)
export CUDA_HOME := env("CUDA_HOME", "/usr/local/cuda-13.0")

[no-cd]
test *args:
    uv run pytest src/mzoo/kernels {{args}}
